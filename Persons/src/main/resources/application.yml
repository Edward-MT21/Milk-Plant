server:
  port: 9090
spring:
  application:
    name: "Persons"
  profiles:
    active: "qa"
  datasource:
    url: jdbc:mysql://localhost:3307/persons-db
    username: root
    password: root
  jpa:
    show-sql: true
  sql:
    init:
      mode: always
  config:
    import: "optional:configserver:http://localhost:8071/"

management:
  endpoints:
    web:
      exposure:
        include: "*"
