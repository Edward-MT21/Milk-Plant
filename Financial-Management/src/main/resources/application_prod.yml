server:
  port: 7070
spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driverClassName: org.h2.Driver
    username: sa
    password: ''
  h2:
    console:
      enabled: true
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
    show-sql: true
  config:
    activate:
      on-profile: "prod"

build:
  version: 1.0

financial-management-contacts:
  message: "Financial-Management Production"
  contactDetails:
    name: "Edward Malte"
    email: "lGm4o@example.com"
  onCallSupport:
    - 317888
    - 317999
