spring:
    application:
        name: "Config-Server"
    profiles:
        active: native
    cloud:
      server:
        native:
          search-locations: "classpath:/config"

server:
    port: 8071
